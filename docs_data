Docs data

App has three methods: Router, createServer, start, 

set Router  with var myRouter = new.app.Router() to establish connection to router methods

app.createServer(Router) -- will create a server with the router functions from Router

app.start(port number, function for where server is running)

myRouter.get('/tony', function(req, res) {
	res.CLMsend(200, 'boyfriend')
})